# BedrockMate 2025 (Hybrid Edition)

🎮 **Minecraft Bedrock Edition (1.22+)** 向けの便利ツール集

小中学生のiPadユーザーをターゲットにした、使いやすいマインクラフト支援ツールです。

## � Tier構成

| Tier | 必要環境 | 機能 |
|------|---------|------|
| **Tier 1** | ブラウザのみ | 座標計算、スライムレーダー、建築ヘルパー |
| **Tier 2** | Python + FastAPI | シード管理、ブックマーク保存 |
| **Tier 3** | Rust CLI | 構造物検索、バイオーム検索 |

---

## 🚀 起動方法

### Tier 1 のみ使う場合（静的サイト）

```bash
git clone https://github.com/t2k2pp/mnecrafttools.git
cd mnecrafttools
npx serve .
```
→ http://localhost:3000 でアクセス

> **Note**: Tier 1はGitHub Pages（https://t2k2pp.github.io/mnecrafttools/）でも利用可能です。

---

### Tier 2 も使う場合（FastAPIサーバー）

シード管理やブックマーク機能を使いたい場合は、FastAPIサーバーを起動します。

#### 1. 必要なパッケージをインストール

```bash
cd mnecrafttools/server
pip install -r requirements.txt
```

#### 2. サーバーを起動

```bash
cd mnecrafttools/server
python main.py
```

→ http://localhost:8000 でアクセス

> **Important**: Tier 2を使う場合は`npx serve`ではなく、上記のFastAPIサーバーを使ってください。
> FastAPIサーバーはTier 1の機能も含めて提供します。

---

### Tier 3 も使う場合（Rust CLI + ジョブシステム）

構造物検索やバイオーム検索を使いたい場合は、Rust CLIをビルドします。

#### 1. Rust CLIをビルド

```bash
cd mnecrafttools/rust-cli
cargo build --release
```

#### 2. FastAPIサーバーを起動（Tier 2と同じ）

```bash
cd mnecrafttools/server
python main.py
```

#### 3. 直接CLIを使う場合

```bash
# 構造物検索
./rust-cli/target/release/bedrockmate structures --seed 12345 --radius 3000

# ネザー構造物検索
./rust-cli/target/release/bedrockmate nether --seed 12345 --radius 500

# バイオーム検索
./rust-cli/target/release/bedrockmate biome --seed 12345 --target jungle --radius 10000
```

---

## 🛠️ 機能一覧

### Tier 1: どこでもツール

| ツール | 説明 |
|--------|------|
| 📍 座標計算 | チャンク・リージョン・ネザー座標を一括計算 |
| 🌀 ネザーポータル | ポータルの最適な位置を計算 |
| 🟢 スライムレーダー | スライムチャンクを探す（**シード不要！**） |
| 📏 距離計算 | 2点間の距離とエリトラ所要時間 |
| ⭕ 建築ヘルパー | 円・球体・楕円のブロック配置図 |

### Tier 2: データ管理

| 機能 | 説明 |
|------|------|
| 🔐 シード管理 | ワールドのシード値を保存・切り替え |
| 📌 ブックマーク | 座標を保存してすぐに参照 |

### Tier 3: 構造物ファインダー

| コマンド | 説明 |
|----------|------|
| `structures` | 村、海底神殿、森の洋館等を検索 |
| `nether` | ネザー要塞、バスティオンを検索 |
| `biome` | 指定バイオームの最寄り座標を検索 |

---

## 📁 プロジェクト構成

```
mnecrafttools/
├── index.html           # Tier 1 メインページ
├── css/style.css
├── js/
│   ├── app.js
│   └── tools/           # Tier 1 ツール
├── server/              # Tier 2 FastAPI
│   ├── main.py          # ← サーバー起点
│   ├── database.py
│   ├── requirements.txt
│   └── routers/
└── rust-cli/            # Tier 3 Rust CLI
    ├── Cargo.toml
    └── src/
```

---

## 技術スタック

- **Frontend**: HTML5, Tailwind CSS, Vanilla JS, htmx
- **Backend**: Python FastAPI, SQLite
- **Compute**: Rust (clap, serde)

## ライセンス

MIT License

---

Made with ⛏️ for Minecraft 冒険者たち
